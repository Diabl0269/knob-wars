{
  "name": "knob-wars",
  "version": "0.1.0",
  "private": true,
  "description": "Multiplayer ear-training synth game. Hear it, rebuild it, beat the room.",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "tunnel": "cloudflared tunnel --url http://localhost:3000"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "latest",
    "qrcode": "^1.5.4",
    "ws": "^8.18.0"
  },
  "engines": { "node": ">=22" }
}
